<div class="col-md-12">
  <div class="job">
    <%= link_to("New Job",new_admin_job_path,class:"btn btn-primary pull-right") %>
  </div>
  <table class="table table-hover">
    <thead>
      <tr>
        <td>#</td>
        <td>Title</td>
        <td>Description</td>
        <td>Wage Lower Bound</td>
        <td>Wage Upper Bound</td>
        <td>Contact Email</td>
        <td>Time</td>
        <td>Creator</td>
      </tr>
    </thead>
    <tbody>
      <% @jobs.each do |job| %>
      <tr>
        <td>#</td>
        <td><%= link_to(job.title,admin_job_path(job))%></td>
        <td><%= simple_format(job.description) %></td>
        <td><%= job.wage_lower_bound%></td>
        <td><%= job.wage_upper_bound%></td>
        <td><%= job.contact_email%></td>
        <td><%= job.created_at%></td>
        <td><%= job.user.email%></td>
        <td><%= link_to("Edit",edit_admin_job_path(job),class:"btn btn=sm btn-default")%></td>
        <td><%= link_to("Delete",admin_job_path(job),class:"btn btn=sm btn-default",method: :delete, data:{confirm:"Are you sure?"})%></td>
      </tr>
      <% end %>

    </tbody>
  </table>
</div>
